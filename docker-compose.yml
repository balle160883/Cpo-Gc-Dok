version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    restart: always
    environment:
      - PORT=4000
      - DATABASE_URL=${DATABASE_URL:-postgresql://postgres:Seguridad2028%40@31.97.144.6:5435/postgres}
      - JWT_SECRET=cobranza-pro-secret-2026
      - CORS_ORIGIN=http://cobranza.31.97.144.6.sslip.io,https://cobranza.31.97.144.6.sslip.io,http://localhost:3000

  frontend:
    build:
      context: ./frontend
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:4000}
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - backend
